# AGENTS.md — Mirror Agent

本文件不单独维护一份工作说明，避免与 `CLAUDE.md` 出现两套互相矛盾的版本。

所有 agent 的工作约定、入口文档、目录结构、验证命令和禁止事项，一律以 `CLAUDE.md` 为准。

请先阅读：

1. `CLAUDE.md`
2. `docs/00-task-progress.md`

改动剧情或结局前，另见 `story-source/`（已确认源稿）与 `docs/06-story-ending-data-format.md`（数据格式）。
